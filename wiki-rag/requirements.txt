streamlit>=1.32.0
httpx>=0.27.0
chromadb>=0.4.22
requests>=2.31.0
python-dateutil>=2.8.2
# sqlite3 is built-in — no install needed
# wikipedia package is NOT used — fetching is done via MediaWiki Action API (requests)
